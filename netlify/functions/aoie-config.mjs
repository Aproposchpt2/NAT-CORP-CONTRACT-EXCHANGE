const CORS={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET,OPTIONS'};
export const handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers:CORS,body:''};if(event.httpMethod!=='GET')return{statusCode:405,headers:CORS,body:JSON.stringify({ok:false,error:'GET only'})};return{statusCode:200,headers:{...CORS,'Cache-Control':'no-store'},body:JSON.stringify({ok:true,aoie_capability_profile_v1:String(process.env.AOIE_CAPABILITY_PROFILE_V1||'false').toLowerCase()==='true',visit_scoped:true,legacy_rollback_available:true})}};
